type Query {
    games: [Game]!
}

type Game {
    id: String!
    background_image: String!
    description: String!
    publishers: [Publisher]!
    rawg_id: Int!
    released: String!
    title: String!
}

type Publisher {
    id: String!
    image: String!
    name: String!
}

